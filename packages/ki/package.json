{
  "name": "@suite/ki",
  "version": "1.0.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "description": "Anbindung an Sprachmodelle — plattformfreies, geteiltes Paket",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./einstellungen": "./src/einstellungen.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node scripts/build-tests.mjs && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.123.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.7.2"
  }
}
