{
  "name": "@suite/eintraege",
  "version": "0.2.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "description": "Was ein Werkzeug abgelegt hat, in einer Form, die alle verstehen — plattformfrei",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node scripts/build-tests.mjs && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.7.2"
  }
}
