// Sammelpunkt fuer die Tests: nur die reinen Module, ohne Electron.
export { alleRegeln, regelNach, absaetze, ARTEN, ART_NAME } from '../src/shared/bestand';
export { finde, schluessel } from '../src/shared/suche';
