# Im Repository immer LF, im Arbeitsverzeichnis das, was die Plattform erwartet.
# Ohne das landen von Windows aus CRLF-Zeilenenden im Repo und die Linux-CI
# sowie Claude Code on the web sehen bei jeder Datei Änderungen.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.woff binary
*.woff2 binary
*.ttf binary
*.ttmap binary
